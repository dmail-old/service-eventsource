import History from '../lib/history.js';

export function suite(add){
	add('add', function(test){
		// check history.limit is respected too
	});

	add('since', function(test){

	});

	add('clear', function(test){

	});
}